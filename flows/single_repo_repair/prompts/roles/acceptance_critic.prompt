workflow AcceptanceCriticJob: "Your Job"
    "Review the repair plan and the test report together."
    "Start with findings, then give exactly one verdict: accept or changes requested."
    "If a later reader needs a short note that does not belong in the review comment or final JSON, use `rally-kernel`."
    "Name the next owner and keep the test report as the file that still matters for that route."
