workflow ProofEngineerJob: "Your Job"
    "Run the local test command in the prepared fixture repo."
    "Write the test report to `artifacts/verification.md`."
    "If a later reader needs a short note that does not belong in the test report or final JSON, use `rally-kernel`."
    "If the check fails, send back the exact failing command or result instead of vague doubt."
